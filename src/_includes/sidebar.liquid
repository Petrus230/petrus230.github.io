{% assign user_url = site.url | append: site.baseurl %}

<div class="sidebar">
  <div class="container sidebar-sticky">
    <div class="sidebar-about">
      <h1>
        <a href="{{ user_url }}">
          {{ site.title }}
        </a>
      </h1>
      <p class="lead">{{ site.description }}</p>
    </div>

    <nav class="sidebar-nav">
      {% assign pages_list = site.pages %}
      {% for node in pages_list %}
        {% if node.title != null %}
          {% if node.layout != 'default' %}
            <a class="sidebar-nav-item{% if page.url == node.url %} active{% endif %}" href="{{ node.url }}">{{ node.title }}</a>
          {% endif %}
        {% endif %}
      {% endfor %}
    </nav>

    <div class="sidebar-icons">
      <a href="{{ user_url }}{{ site.baseurl }}/atom.xml" class="icon icon-rss" title="RSS Feed"></a>
      <div class="icon inline-block contact-info">
        {% include linkedin.liquid %}
      </div>
      <div class="icon inline-block contact-info">
        {% include email.liquid %}
      </div>
      <div class="language-menu">
        {% for tongue in site.languages %}
          <a
            {% if tongue == site.active_lang %}
              style="font-weight: bold;"
            {% endif %}
            {% static_href -%}
            href="{% if tongue == site.default_lang %}{{site.baseurl}}{{page.url}}{% else %}{{site.baseurl}}/{{ tongue }}{{page.url}}{% endif %}"
            {% endstatic_href %}
          >
            {{- tongue -}}
          </a>
          {%- if forloop.last == false -%}
            {{ ' ' -}}
            {{- site.langsep }}
          {%- endif -%}
        {% endfor %}
      </div>
    </div>
    <!-- Print-specific information -->
    <div class="tecnologies-info">
      <h4>Tecnologies</h4>
      <p>Python - Ruby - Typescript - C++</p>

      <p>Django - NodeJs - PostgreSQL - Kibana - Elasticsearch - Redis</p>

      <p>React - Vue - CSS - HTML</p>

      <p>Docker - Kubernetes - Ansible - AWS</p>

      <p>GeoJson - EPANET - TETRA</p>

      <p>Linux - Bash - Git</p>
      <p></p>

      <h4>Languages</h4>
      Catalan Spanish English

      <div class="hobbies-info">
        <h4>Hobbies</h4>
        <p>🪚 Woodwork</p>
        <p>🍲 Eat</p>
        <p>🔪 Cook</p>
        <p>🌿 Plants</p>
      </div>
    </div>
  </div>
</div>
