{% assign user_url = site.url | append: site.baseurl %}

<div class="sidebar">
  <div class="container sidebar-sticky">
    <div class="sidebar-about">
      <h1>
        <a href="{{ user_url }}">
          {{ site.title }}
        </a>
      </h1>
      <p class="lead">{{ site.description }}</p>
    </div>

    <nav class="sidebar-nav">
      {% assign pages_list = site.pages %}
      {% for node in pages_list %}
        {% if node.title != null %}
          {% if node.layout != 'default' %}
            <a class="sidebar-nav-item{% if page.url == node.url %} active{% endif %}" href="{{ node.url }}">{{ node.title }}</a>
          {% endif %}
        {% endif %}
      {% endfor %}
    </nav>

    <div class="sidebar-icons">
      <a href="{{ user_url }}{{ site.baseurl }}/atom.xml" class="icon icon-rss" title="RSS Feed"></a>
      <div class="icon inline-block print-info">
        {% include linkedin.liquid %}
      </div>
      <div class="icon inline-block print-info">
        {% include email.liquid %}
      </div>
    </div>
    <!-- Print-specific information -->
    <div class="print-info">
      <p>Email: <a href="mailto:{{ site.user_email }}">{{ site.user_email }}</a></p>
      <p>LinkedIn: <a href="https://linkedin.com/in/{{ site.linkedin_username }}">{{ site.linkedin_username }}</a></p>
    </div>
  </div>
</div>
